---
title: "Code Tabs Demo"
summary: "Tabbed, copyable code blocks (LeetCode-style) using a reusable component."
date: "2025-09-06"
tags:
  - Code
  - UI
  - Components
draft: false
---

import CodeTabs from "@components/CodeTabs"

# Code Tabs Demo

Below is a reusable code-tabs component with a copy button. You can use it in any `.mdx` blog post.

<CodeTabs client:load tabs={[
  {
    name: "C#",
    lang: "csharp",
    code: `public class Solution {
  public int[] TwoSum(int[] nums, int target) {
    for (int i = 0; i < nums.Length; i++) {
      for (int j = i + 1; j < nums.Length; j++) {
        if (nums[j] == target - nums[i]) {
          return new int[] { i, j };
        }
      }
    }
    return new int[] {};
  }
}`
  },
  {
    name: "JavaScript",
    lang: "javascript",
    code: `function twoSum(nums, target) {
  for (let i = 0; i < nums.length; i++) {
    for (let j = i + 1; j < nums.length; j++) {
      if (nums[j] === target - nums[i]) return [i, j];
    }
  }
  return [];
}`
  },
  {
    name: "Python3",
    lang: "python",
    code: `def twoSum(nums, target):
    for i in range(len(nums)):
        for j in range(i + 1, len(nums)):
            if nums[j] == target - nums[i]:
                return [i, j]
    return []`
  },
  {
    name: "Go",
    lang: "go",
    code: `package main

func twoSum(nums []int, target int) []int {
    for i := 0; i < len(nums); i++ {
        for j := i + 1; j < len(nums); j++ {
            if nums[j] == target-nums[i] {
                return []int{i, j}
            }
        }
    }
    return []int{}
}`
  }
]} />

Tip: To use this component in other posts, switch the file extension to `.mdx`, import the component at the top, and pass your languages and code as shown above.

